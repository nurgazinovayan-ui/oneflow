node_modules
dist
dist-electron
oneflow-web
release
*.log

# Build artifacts — regenerated by npm run dist / build:web, not source
*.exe
*.zip

# Secrets / environment — .env.web holds only publishable keys today, but keep the whole
# family out of git as a habit; ship .env.web.example instead for anyone setting up fresh.
.env
.env.*
!.env.*.example

# OS cruft
.DS_Store
Thumbs.db
